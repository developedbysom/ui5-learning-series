<mvc:View 
controllerName="dataBinding.controller.App"
xmlns="sap.m" xmlns:form="sap.ui.layout.form" 
xmlns:l="sap.ui.layout"
xmlns:core="sap.ui.core"
xmlns:mvc="sap.ui.core.mvc">

<Panel headerText="Binding Fundamentals" class="sapUiResponsiveMargin" width="auto">
    <form:SimpleForm editable="true" layout="ColumnLayout" >
        <Label text="First Name"/>
        <Input value="{employee>/firstName}" valueLiveUpdate="true" width="200px" enabled="{employee>/enabled}"/>
        <Label text="Last Name"/>
        <Input value="{employee>/lastName}" valueLiveUpdate="true" width="200px" enabled="{employee>/enabled}"/>
        <Label text="Enabled"/>
        <CheckBox selected="{employee>/enabled}"/>
		<Label text="Full Name"/>
		<Text text="{ parts:[
								{path: 'employee>/firstName'},
								{path: 'employee>/lastName'}
							] }"/>
        
    </form:SimpleForm>

</Panel>
<Panel headerText="Address" class="sapUiResponsiveMargin" width="auto">
    <content >  
 
        <Label text="Home Address" showColon="true"/>    
        <!-- <Input binding="{employee>/Address}" value="{path: 'employee>street'}"/>   This also works  -->
        <Input binding="{employee>/Address}" value="{employee>street}"/>    
        <FormattedText class="sapUiSmallMarginBottom" binding="{employee>/Address}"
					htmlText="{employee>street}&lt;br>{employee>City} &lt;br>{employee>Country}"
					id="address" 
					width="200px"/>
    </content>

</Panel>
<!-- <Panel headerText="Validation" class="sapUiResponsiveMargin" width="auto">
    <content >  
 
        <Label text="POC Constraints" showColon="true"/>    
        <!-- <Input binding="{employee>/Address}" value="{path: 'employee>street'}"/>   This also works  -->
        <Input id='amt' class="sapUiSmallMarginBottom" placeholder="Enter Order Quantity"
				valueStateText="Quantity should be between 1 and 10"
                value="{
                path:'Constraints>/OrderQuantity',
                type: 'sap.ui.model.type.Integer',                
                constraints : {
					minimum: 1,
					maximum: 10
                    }
				}"
        			change=".onChange"/>

        	<Input
				id="nameInput"
				class="sapUiSmallMarginBottom"
				placeholder="Enter name"
				valueStateText="Name must not be empty. Maximum 10 characters."
				value="{
					path : 'Constraints>/OrderQuantity',
					type : 'sap.ui.model.type.Integer',
					constraints : {
						minimum: 1,
						maximum: 10
					}
				}"
				change= ".onNameChange1" />
    </content>
	<DatePicker
			id="DP4"
			dateValue="{path:'Constraints>/date', 
					type:'sap.ui.model.type.Date', 
					formatOptions: 
					{ style: 'long', strictParsing: true}
				}"
			class="sapUiSmallMarginBottom" width='300px'/>

	<!-- <DatePicker id="datPicker" value="{ 
									path: 'Constraints>/date' ,
									type:'sap.ui.model.type.Date',
									formatOptions:{
										pattern:'yyyy/MM/dd',
										strictParsing:'true'
									}
										}" width='200px'/> -->

</Panel> -->
<Panel headerText="Product List"  class="sapUiResponsiveMargin" width="auto">
    <List items="{products>/Products}">
        <ObjectListItem title="{products>ProductName}"
        press=".onPressItem"
        type="Active"        
        number="{
            parts:[{path:'products>UnitPrice'},{path:'products>currencyCode'}],
            type:'sap.ui.model.type.Currency',
            formatOptions:{showMeasure:false}
        }"        
        numberUnit="{products>currencyCode}" 
        numberState="{= ${products>UnitPrice} > ${employee>/thresholdValue} ? 'Error': 'Success' }"
        >
        <attributes>
            <ObjectAttribute text="{products>QuantityPerUnit}"/>   
        </attributes>
        
        </ObjectListItem>
    </List>
</Panel>
<Panel id="productDetailsPanel" headerText="Product Details" class="sapUiResponsiveMargin" width="auto"
binding="{products>/Products/1}">
<form:SimpleForm editable="true" layout="ColumnLayout">
			<Label text="{i18n>ProductID}"/>
			<Input value="{products>ProductID}"/>

			<Label text="{i18n>ProductName}"/>
			<Input value="{products>ProductName}"/>

			<Label text="{i18n>QuantityPerUnit}"/>
			<Input value="{products>QuantityPerUnit}"/>

			<Label text="{i18n>UnitPrice}"/>
			<Input value="{products>UnitPrice}"/>

			<Label text="{i18n>UnitsInStock}"/>
			<Input value="{products>UnitsInStock}"/>

			<Label text="{i18n>Discontinued}"/>
			<CheckBox selected="{products>Discontinued}"/>
		</form:SimpleForm>

</Panel>

<Panel headerText="List with Factory Function}" class="sapUiResponsiveMargin" width="auto">
		<List
			id="ProductList"
			headerText="{i18n>productListTitle}"
			items="{
				path: 'products>/Products',
				factory: '.productListFactory'
			}">
			<dependents>
				<core:Fragment fragmentName="dataBinding.view.ProductSimple" type="XML"/>
				<core:Fragment fragmentName="dataBinding.view.ProductExtended" type="XML"/>
			</dependents>
		</List>
</Panel>

    <Text text="Hello {/greeting}" />
</mvc:View>